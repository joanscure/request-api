{
  "name": "http-local",
  "version": "0.1.0",
  "description": "Cliente HTTP local — sin login, sin nube",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.joanscure.request-api",
    "productName": "Request API",
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico"
    }
  }
}
